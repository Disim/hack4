import 'package:flutter/material.dart';

class AppealAdding extends StatelessWidget {
  const AppealAdding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
